version: 2
sources:
  - name: raw_x_ads
    schema: main
    tables:
      - name: raw_x_ads_daily
models:
  - name: stg_x_ads_daily
    columns:
      - name: account_id
        tests: [not_null]
      - name: entity_id
        tests: [not_null]
      - name: metric
        tests: [not_null]
      - name: provider_value
        tests: [not_null]
      - name: pull_id
        tests: [not_null]
      - name: project_id
        tests: [not_null]
    tests:
      - unique:
          name: stg_x_ads_full_grain_unique
          arguments:
            column_name: "project_id || '|' || report_profile || '|' || account_id || '|' || entity_type || '|' || entity_id || '|' || placement || '|' || segment_type || '|' || segment_value || '|' || interval_start || '|' || interval_end || '|' || metric"
