version: 2
sources:
  - name: raw_sa360
    schema: main
    tables:
      - name: raw_sa360_daily
models:
  - name: stg_sa360_daily
    columns:
      - name: report_profile
        tests: [not_null]
      - name: customer_id
        tests: [not_null]
      - name: metric
        tests: [not_null]
      - name: provider_value
        tests: [not_null]
      - name: pull_id
        tests: [not_null]
      - name: project_id
        tests: [not_null]
    tests:
      - unique:
          name: stg_sa360_full_grain_unique
          arguments:
            column_name: "project_id || '|' || report_profile || '|' || customer_id || '|' || resource_id || '|' || COALESCE(date, '') || '|' || dimensions_json || '|' || metric"
