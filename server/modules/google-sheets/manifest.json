{
  "schema_version": "1.2",
  "name": "google-sheets",
  "display_name": "Google Sheets",
  "public_catalog": {
    "visibility": "public",
    "category": "files_warehouses",
    "onboarding_modes": [
      "managed_feed"
    ],
    "verification": {
      "status": "blocked",
      "reason_code": "live_evidence_not_ratified",
      "follow_up": "Ratify and run the independent connector evidence contract."
    }
  },
  "module_kind": "kpi",
  "auth_type": "google_direct",
  "report_profiles": [
    {
      "id": "sheet_daily",
      "raw_relation": "raw_google_sheets_daily",
      "staging_relation": "stg_google_sheets_daily",
      "display_name": "Sheet Data (daily)",
      "metrics": [
        "budget_declared",
        "target_revenue",
        "target_conversions"
      ],
      "dimensions": [
        "date",
        "sheet_row_id"
      ],
      "extraction_capabilities": {
        "row_limit": null,
        "filters_supported": false,
        "realtime": false
      },
      "extraction_path": "custom_pull",
      "verification_expected_rows_per_day": null,
      "_ai_53_notes": "AI-53 VERIFIE le 2026-07-19 : Google Sheets API v4. Endpoint GET https://sheets.googleapis.com/v4/spreadsheets/{spreadsheetId}/values/{range} (values.get) pour une plage unique ; batchGet pour plusieurs plages. Le scope OAuth requis est 'https://www.googleapis.com/auth/spreadsheets.readonly' -- il fait partie du GOOGLE_STACK_SCOPES de l'Epic 18 (AD-21). Quota : 300 requetes par minute par projet, 60 requetes par minute par utilisateur (doc officielle Google Sheets API v4, https://developers.google.com/sheets/api/limits, verifiee le 2026-07-19). Une extraction de feuille typique = 1 requete API (batchGet de toute la plage) -- la limite de quota est tres confortable. HTTP 429 -> RateLimitError('google-sheets', retry_after). A CONFIRMER en passe live (Phase B, AI-13/AI-08)."
    }
  ],
  "canonical_metric_mapping": {
    "budget_declared": "budget_declared",
    "target_revenue": "target_revenue",
    "target_conversions": "target_conversions"
  },
  "canonical_dimension_mapping": {
    "date": "date",
    "sheet_row_id": "sheet_row_id"
  },
  "column_mapping_schema": {
    "_doc": "Le column_mapping est DECLARE PAR DATASTREAM (config), pas en dur dans le code. Chaque connexion Google Sheets declare quelles colonnes de sa feuille correspondent a quelles metriques/dimensions. Ce schema documente la structure attendue du mapping.",
    "_structure": {
      "date_column": "Nom de la colonne contenant la date (ex: 'Date', 'Periode'). Obligatoire.",
      "row_id_column": "Nom de la colonne servant d'identifiant de ligne (ex: 'Campagne', 'Canal'). Optionnel -- si absent, l'index de la ligne est utilise.",
      "metric_columns": {
        "_doc": "Dictionnaire {nom_colonne_sheet: nom_metrique_canonique}. Ex: {'Budget': 'budget_declared', 'Objectif CA': 'target_revenue'}.",
        "_canonical_metrics": [
          "budget_declared",
          "target_revenue",
          "target_conversions"
        ],
        "_note": "Seules les metriques listees dans canonical_metric_mapping sont acceptees comme valeurs."
      }
    },
    "_validation": "Une colonne manquante dans la feuille = erreur explicite (AD-9 : jamais 0 invente). Les lignes non parsables (valeur non numerique dans une colonne metrique) sont comptees et loggees -- jamais silencieuses.",
    "_number_format": "Format numerique accepte dans les cellules metriques. Heuristique : le DERNIER separateur (point ou virgule) est le separateur decimal. Exemples : '1.234,56' -> 1234.56 (FR : derniere sep = ',') ; '1,234.56' -> 1234.56 (EN : derniere sep = '.') ; '5 000,50' -> 5000.50 (FR : seule virgule = decimal) ; '1234.56' -> 1234.56 (EN direct). Les espaces normaux et insecables sont toujours retires avant le parsing (ex: '5 000,50' -> '5000,50' -> 5000.50)."
  },
  "auth": {
    "type": "google_direct",
    "auth_path": "google_direct",
    "scope": "https://www.googleapis.com/auth/spreadsheets.readonly",
    "_note": "AD-21 : le scope spreadsheets.readonly fait partie du GOOGLE_STACK_SCOPES de l'Epic 18. La connexion Google Sheets utilise auth_path='google_direct' dans app.connection_ref. get_fresh_token(connection_id) route automatiquement sur le token_service de l'Epic 18.3 -- le module n'a pas a s'en preoccuper. PAS de Nango pour les connecteurs du stack Google (directive Jean 2026-07-18).",
    "_ai_53_notes": "AI-53 : le scope 'spreadsheets.readonly' est documente dans la documentation officielle Google Sheets API v4 (https://developers.google.com/sheets/api/guides/authorizing). Il est inclus dans GOOGLE_STACK_SCOPES (server/core/google_oauth.py). A CONFIRMER que le consentement unique Epic 18 couvre bien ce scope en passe live (Phase B)."
  },
  "quota": {
    "window_seconds": 60,
    "budget_points": 60,
    "read_cost": 1,
    "write_cost": 1,
    "_write_cost_note": "Module lecture seule (readonly scope) -- write_cost jamais depense; le schema manifest exige une valeur strictement positive (exclusiveMinimum 0), valeur formelle alignee sur google-analytics/gsc.",
    "_ai_53_notes": "AI-53 VERIFIE le 2026-07-19 : Google Sheets API v4 applique des limites en nombre de requetes : 300 requetes/min par projet (global), 60 requetes/min par utilisateur. Une extraction typique d'une feuille = 1 appel batchGet. Profil retenu : window 60s / 60pts / read_cost=1 (1 requete par pull = 1 point). La limite est tres confortable pour l'usage de saisie d'objectifs. HTTP 429 -> RateLimitError('google-sheets', retry_after) comme les autres connecteurs. Source officielle : https://developers.google.com/sheets/api/limits. A CONFIRMER en passe live."
  },
  "widget_ref": "ui://core/daily-report",
  "error_map": {
    "401:authError": "auth_expired",
    "401:required": "auth_expired",
    "401:UNAUTHENTICATED": "auth_expired",
    "403:PERMISSION_DENIED": "permission_denied",
    "403:forbidden": "permission_denied",
    "403:insufficientPermissions": "permission_denied",
    "403:appNotAuthorizedToFile": "permission_denied",
    "403:domainPolicy": "permission_denied",
    "403:dailyLimitExceeded": "provider_transient",
    "403:rateLimitExceeded": "provider_transient",
    "403:userRateLimitExceeded": "provider_transient",
    "400:badRequest": "invalid_request",
    "400:INVALID_ARGUMENT": "invalid_request",
    "400:invalidParameter": "invalid_request",
    "500:INTERNAL": "provider_transient",
    "500:internalError": "provider_transient",
    "503:UNAVAILABLE": "provider_transient",
    "503:backendError": "provider_transient"
  },
  "_error_map_note": "Codes are the reason strings of the standard Google Workspace / API Console error envelope, as the Sheets API v4 returns it (https://developers.google.com/sheets/api/errors and the shared reference https://developers.google.com/drive/api/guides/handle-errors), together with the google.rpc status enums the same surface uses (https://cloud.google.com/apis/design/errors). SHAPE THE KEYS MATCH: { error: { code: <numeric http>, message, status: '<ENUM>', errors: [{ message, domain, reason: '<reason>' }] } } -- core.pull_errors._extract_provider_codes yields errors[].reason first, then error.status, then error.code. error.code is never keyed on: it repeats the HTTP status. WHERE THE MAP IS CONSULTED, AND WHERE THE LEGACY CONTRACT WINS: _fetch_sheet_values / _fetch_sheet_metadata still raise Python PermissionError on a 403 and FileNotFoundError on a 404, because core/google_sheets_sync.py classifies the adapter exception BY TYPE (PermissionError -> revoked_consent, FileNotFoundError -> spreadsheet_not_found) and that contract is not this repair's to break. The classifier now runs FIRST on every non-429 status: for a 403 the typed error is raised in place of PermissionError only when the map made it RETRYABLE (the quota reasons below), because 'the quota is spent' was being reported to the operator as 'consent revoked'; every other 403 keeps PermissionError, and every status other than 403/404/429 now raises the typed ConnectorError instead of httpx.HTTPStatusError. WHAT THE MAP CHANGES vs the pure-HTTP classification: 403 dailyLimitExceeded / rateLimitExceeded / userRateLimitExceeded become provider_transient instead of permission_denied (Google publishes them as retry-with-backoff), which is the whole reason this map stopped being empty. NO auth_revoked ENTRY, AND THAT IS A MEASURED ABSENCE, NOT AN OVERSIGHT: the two reasons that would earn it (appNotAuthorizedToFile, domainPolicy -- the app grant on that file withdrawn or refused by the domain administrator) arrive on a 403, where this module must keep raising PermissionError so core/google_sheets_sync.py still reads 'revoked consent'; an AuthRevokedError would be classified there as a range drift instead. They are declared as permission_denied, which is what the raise site can honour. No 429 key by construction: HTTP 429 raises core.quota.RateLimitError (breaker path) and a 429 handed to classify_http_error raises ValueError. No 404 key: the 404 branch raises FileNotFoundError before the classifier is consulted, so a key there would be dead.",
  "_account_topology_note": "No account_topology declared. Spreadsheet selection is datastream-level configuration (spreadsheet_id + sheet_range are pull parameters), not a connection-level account hierarchy. A Google Sheets connection authenticates a Google account via google_direct OAuth; there is no sub-account or property to discover or select -- any spreadsheet accessible to the authenticated user can be addressed by its ID at pull time.",
  "_ai_53_notes": {
    "verified_on": "2026-07-19",
    "endpoint": "Google Sheets API v4 -- https://sheets.googleapis.com/v4/spreadsheets/{spreadsheetId}/values/{range} (GET values.get) ou /values:batchGet (GET batchGet). Champ 'values' de la reponse = tableau de lignes (tableau de tableaux de strings). La premiere ligne est supposee etre l'en-tete des colonnes. Source : https://developers.google.com/sheets/api/reference/rest/v4/spreadsheets.values",
    "scope": "spreadsheets.readonly fait partie du GOOGLE_STACK_SCOPES (AD-21). Verifie dans server/core/google_oauth.py GOOGLE_STACK_SCOPES.",
    "mapping_declaratif": "Le mapping colonnes -> metriques est DECLARE dans la config du datastream, pas encode dans le module. Le module lit le mapping depuis les params du pull et valide que les colonnes existent dans la feuille. Une colonne absente = erreur explicite (AD-9). Une valeur non numerique dans une colonne metrique = ligne loggee comme non parsable et exclue (jamais silencieuse).",
    "no_formulas": "Les formules Google Sheets ne sont PAS evaluees : l'API values.get retourne les valeurs calculees (FORMATTED_VALUE ou UNFORMATTED_VALUE) -- ce qui est dans la cellule, pas la formule. La synchronisation bidirectionnelle (ecriture vers Sheets) est HORS SCOPE."
  },
  "source_capabilities": {
    "contract_version": "1",
    "field_discovery": {
      "mode": "runtime",
      "allowed_targets": [
        "budget_declared",
        "target_revenue",
        "target_conversions",
        "date",
        "sheet_row_id"
      ]
    },
    "fields": [
      {
        "field_id": "budget_declared",
        "source_field": "budget_declared",
        "kind": "metric",
        "physical_type": "decimal",
        "description": "budget declared exposed by Google Sheets.",
        "semantic_hints": [
          "spend",
          "currency"
        ],
        "canonical_target": "budget_declared",
        "aggregation": "sum",
        "non_additive": false
      },
      {
        "field_id": "target_revenue",
        "source_field": "target_revenue",
        "kind": "metric",
        "physical_type": "decimal",
        "description": "target revenue exposed by Google Sheets.",
        "semantic_hints": [
          "revenue",
          "currency"
        ],
        "canonical_target": "target_revenue",
        "aggregation": "sum",
        "non_additive": false
      },
      {
        "field_id": "target_conversions",
        "source_field": "target_conversions",
        "kind": "metric",
        "physical_type": "integer",
        "description": "target conversions exposed by Google Sheets.",
        "semantic_hints": [
          "conversion",
          "count"
        ],
        "canonical_target": "target_conversions",
        "aggregation": "sum",
        "non_additive": false
      },
      {
        "field_id": "date",
        "source_field": "date",
        "kind": "dimension",
        "physical_type": "date",
        "description": "date exposed by Google Sheets.",
        "semantic_hints": [
          "date"
        ],
        "canonical_target": "date",
        "aggregation": "none",
        "non_additive": false
      },
      {
        "field_id": "sheet_row_id",
        "source_field": "sheet_row_id",
        "kind": "dimension",
        "physical_type": "string",
        "description": "sheet row id exposed by Google Sheets.",
        "semantic_hints": [
          "identifier"
        ],
        "canonical_target": "sheet_row_id",
        "aggregation": "none",
        "non_additive": false
      }
    ],
    "reports": [
      {
        "id": "sheet_daily",
        "selection_mode": "exact_bundle",
        "availability": {
          "status": "unavailable",
          "reason_code": "managed_sheet_configuration_required",
          "follow_up": "Complete recurring Google Sheets synchronization."
        },
        "metrics": [
          "budget_declared",
          "target_revenue",
          "target_conversions"
        ],
        "dimensions": [
          "date",
          "sheet_row_id"
        ],
        "supported_grains": [
          [
            "date",
            "sheet_row_id"
          ]
        ],
        "compatibility": [],
        "filters": [],
        "pagination": {
          "mode": "none",
          "completeness": "complete",
          "max_pages": null,
          "row_limit": null,
          "truncation_signal": "none"
        },
        "quota_cost": {
          "read_points": 1,
          "unit": "request"
        },
        "incremental": {
          "mode": "date_window",
          "cursor_field": null
        },
        "cadence": {
          "minimum_interval_minutes": 60,
          "supported_modes": [
            "manual",
            "hourly",
            "daily"
          ]
        }
      }
    ]
  },
  "_max_provider_backfill_days_note": "No max_provider_backfill_days is declared: this provider's published reporting window has not been established as of 2026-08-17. The key is left absent rather than filled with an estimate -- an invented re-collection bound promises the operator a history the provider never committed to. Establishing it means reading the provider's own documentation and recording the URL here, as the modules that do declare one have done."
}
