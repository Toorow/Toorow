# A page whose amendment replaces a bullet instead of adding one

## Incomplete if

- the first criterion of the page
- the criterion this page later replaced

### Replaces the last `Incomplete if` bullet

- the replacement, which takes the index the replaced bullet held
