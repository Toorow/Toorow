---
title: Agent Rules & Platform Guarantees
description: How an AI agent must query toorow, and what the platform guarantees in return.
---

An AI agent connected to **toorow** reads governed marketing data. Five rules keep its
answers correct; the guarantees below are what the platform holds up on its side.

---

## Five rules for an agent

### Rule 1 — Discover before you query
Inspect what a source actually offers with `get_source_capabilities(project_id, connection_ref_id)`
before requesting a metric/dimension combination. Never guess a column name or a grain:
a guessed field does not fail loudly, it returns nothing.

### Rule 2 — Never sum a ratio
CTR, CPA, ROAS and Average Position are **not additive**. Summing them across days or
campaigns produces a number that looks plausible and is wrong. Query the semantic ratio
views instead — `semantic_ctr`, `semantic_cpa`, `semantic_roas`, `semantic_avg_position` —
which recompute the ratio at the grain you asked for.

### Rule 3 — Always scope to a project
Every tool call carries an explicit `project_id`. There is no cross-project read: the
isolation is enforced by the database and by the warehouse, not by convention, so an
unscoped call fails rather than leaking.

### Rule 4 — Keep `structuredContent` intact
Answer with a short text summary (≤ 30 lines) **and** pass the full `structuredContent`
payload through untouched. The host renders interactive widgets from that payload; a
summary that replaces it turns a chart into a paragraph.

### Rule 5 — Context events are candidates, never causes
When annotating an anomaly with `context_events`, present them as **possible context**.
Causal phrasing is prohibited — "caused by", "due to", "because of". toorow records that a
deployment and a traffic drop share a date; it does not know that one produced the other.

---

## What the platform guarantees

| | |
| --- | --- |
| **Answers carry their data** | Every tool returns a short text summary plus a full structured payload, so a host can render it instead of re-parsing prose. |
| **Secrets never reach the model** | Tokens live in the secret store. No tool output, log or error message emits one. |
| **Ratios are computed, never stored** | Ratio metrics are derived at query time at the grain you asked for. |
| **Tenant isolation** | Access is rooted at the organization and enforced in the database and in warehouse queries. |
| **Full provenance** | Every extraction is stamped with an append-only identifier, so any figure can be traced back to the pull that produced it. |
| **Grain is preserved** | Raw landings keep metric, date, breakdown dimension and value together — nothing is pre-aggregated on the way in. |
| **Context stays non-causal** | Events are offered as candidate context; the platform never asserts causation. |
| **One currency per project** | Ad spend and revenue are harmonized to the project's target currency, with the rate and its as-of date kept alongside. |
| **No phone-home** | A self-hosted instance ships with zero telemetry. Sending anything is opt-in. |
| **Fresh reads** | A rolling 30-day cache serves agent queries; older ranges read through to the warehouse. |
| **Feedback is recorded** | A +1 / −1 on an analysis is stored and used to benchmark future answers. |
| **Connectors are verified** | No connector ships without passing the platform's conformance suite. |
| **Exports are allow-listed** | What may leave the platform is declared explicitly; anything undeclared is not exported. |
| **Desktop console** | The admin console targets 1280 px and wider. There is no mobile layout. |

---

## Next Steps & Cross-References

<CardGroup cols={2}>
  <Card title="Agent Tools Reference" icon="wrench" href="/agent-tools">
    Inspect FastMCP tool definitions and parameter schemas.
  </Card>
  <Card title="Platform Constraints" icon="shield-check" href="/constraints">
    Review security rules and pre-live checklists.
  </Card>
</CardGroup>
